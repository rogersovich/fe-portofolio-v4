services:
  nuxt:
    build:
      context: .
      dockerfile: Dockerfile
    image: fe-portfolio:latest
    container_name: fe-portfolio
    env_file:
      - .env
    restart: unless-stopped
    expose:
      - "3000"
    networks: [web]

networks:
  web:
    external: true
